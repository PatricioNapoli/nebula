macro(INCLUDE_VENDOR dir)
    file(GLOB children ${dir}/*)
    foreach(child ${children})
        if(IS_DIRECTORY ${child})
            include_directories(${child}/lib/include)
        endif()
    endforeach()
endmacro()

include_directories(vendor/glm/lib)
include_directories(vendor/xxhash/lib)
